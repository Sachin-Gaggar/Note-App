const baseUrl = '487fbcc7d9f6';
const domain = 'ngrok.io';
const subBase = 'api';
export const users = 'users';
export const authenticate = 'authenticate';
export const notes = 'notes';
export const defaultUrl = `https://${baseUrl}.${domain}/${subBase}`;
